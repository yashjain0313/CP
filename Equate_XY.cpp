#include <bits/stdc++.h>
using namespace std;
#define fr(i, n) for (int i = 0; i < (n); i++)
#define endl '\n'
#define int long long
#define arrin(a, n)             \
    for (int i = 0; i < n; i++) \
        cin >> a[i];

void solve()
{
 
}

signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T;
    cin >> T;
    while (T--)
    {
        solve();
    }
    return 0;
}
